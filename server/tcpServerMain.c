#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include "SDL2/SDL_net.h"
#include "tcpServer.h"


/*
int main(int argc, char **argv)
{
    TCPserver *server = getTCPserver();
    bool r = server->init(server);

    if(r) printf("ok\n");

    int running = 1;
    while(running)
    {

        server->TCPlisten(server);
 
    }
    return 0;
}*/